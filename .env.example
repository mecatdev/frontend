# Copy to .env sebelum docker compose up
# URL backend (lokal atau deployed)
# Di Mac/Windows: host.docker.internal mengarah ke host machine
# Di Linux: gunakan IP host atau nama service jika di network yang sama
BACKEND_URL=http://host.docker.internal:4000
